version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosec
    - gocritic
    - dupl
  settings:
    gosec:
      excludes:
        - G304 # File path provided as taint input - we validate save_path
    errcheck:
      check-blank: false
    dupl:
      threshold: 150
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gosec
          - dupl
